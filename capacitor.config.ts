import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aarav.lifesite',
  appName: 'LifeSite',
  webDir: 'dist',
  ios: {
  contentInset: "automatic"
}
};

export default config;
